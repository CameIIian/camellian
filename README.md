# Link集サイト（ターミナル風 / Dracula）

TypeScript + Node.js で作成した、リンク集のシンプルなWebサイトです。

## セットアップ

```bash
npm run build
npm start
```

`http://localhost:3000` にアクセスしてください。

## 開発

```bash
npm run check
npm run build
```

## リンクの追加

`src/links.ts` を編集すると一覧が更新されます。
