{
  "name": "website-link-collection",
  "version": "1.0.0",
  "description": "Terminal-style Dracula themed link collection site",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsc && node dist/server.js",
    "build": "tsc",
    "start": "node dist/server.js",
    "check": "tsc --noEmit",
    "build:cloudflare": "npm run build && node dist/build-cloudflare.js",
    "preview:cloudflare": "npx wrangler pages dev cloudflare-dist"
  },
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.9.3"
  }
}
